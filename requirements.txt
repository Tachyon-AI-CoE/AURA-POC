# AURA Orchestrator — production dependencies
# Keep in sync with pyproject.toml [project.dependencies]

fastapi==0.111.0
uvicorn[standard]==0.29.0
PyJWT[crypto]==2.8.0
requests==2.31.0
pydantic>=2.7
pydantic-settings>=2.2
